# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual env
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Results (experiment output)
results/*.parquet
results/*.csv
results/figures/
results/SUMMARY.md
results/VALIDATION.md

# LaTeX validation script (tracked separately for documentation)
!validate_latex.py

# LaTeX files (synced to Overleaf - do not ignore, but track)
# *.tex files are managed via Overleaf sync

# LaTeX build byproducts -- generated by `make` / tectonic, never meaningful
# to commit. PDFs are tracked (they need to sync across machines).
research/*.aux
research/*.bbl
research/*.blg
research/*.log
research/*.out

# uv
uv.lock

# OS
.DS_Store

# Git hooks
.git/hooks/
